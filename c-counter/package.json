{
   "scripts": {
      "test": "ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts",
      "build:deploy": "rm -rf ./out/ && make && solana program deploy ./out/program.so",
      "build:deploy:logs": "yarn build:deploy | tee deploy_output.log && id=$(grep 'Program Id:' deploy_output.log | awk '{print $3}') && solana logs | grep $id -A 10"
    },
  "devDependencies": {
    "@solana/web3.js": "^1.95.3",
    "@types/bn.js": "^5.1.0",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.0.0",
    "buffer": "^6.0.3",
    "chai": "^4.3.4",
    "fs": "^0.0.1-security",
    "mocha": "^9.0.3",
    "path": "^0.12.7",
    "prettier": "^2.6.2",
    "ts-mocha": "^10.0.0",
    "typescript": "^4.3.5"
  }
}
